<% if false %>
  <img src="//<%= ENV["CLOUDFRONT_DOMAIN_NAME"] %>/logo.png" class="logo">
  <img src="//<%= ENV["CLOUDFRONT_DOMAIN_NAME"] %>/logo.png" class="logo">
<% end %>
  <%= image_tag "logo.png", class: "logo" %>
<br>
<% if current_user %>
  Logged in as <%= current_user.email %>.
  <%= link_to "Logout", logout_path %>
<% else %>
  <%= link_to "Register", register_path %> or
  <%= link_to "Login", login_path %>
<% end %>
